# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB
MONGO_URI=mongodb+srv://vipinkalyan28_db_user:6jsT2afyXbpnLBb4@smartattendanceapp.sjljwn2.mongodb.net/?appName=SmartAttendanceApp

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# QR token (short-lived rotating token embedded in QR image)
QR_TOKEN_SECRET=change_this_to_another_long_random_secret
QR_TOKEN_TTL_SECONDS=40
ATTENDANCE_SESSION_MINUTES=10

# Geofencing
MAX_ALLOWED_DISTANCE_METERS=50
MAX_ALLOWED_GPS_ACCURACY_METERS=100

# Attendance threshold for defaulter alerts
LOW_ATTENDANCE_THRESHOLD_PERCENT=75

# Initial admin (used by seed/seedAdmin.js)
ADMIN_NAME=Super Admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe@123

# Email (SMTP) - e.g. Gmail app password, Mailtrap, SendGrid SMTP, etc.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
EMAIL_FROM="Smart Attendance <no-reply@example.com>"

# Cron schedule for low-attendance check (default: every day at 7am)
LOW_ATTENDANCE_CRON=0 7 * * *


